<UserControl x:Class="Hub.Controls.DictionarySettingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <DataGrid ItemsSource="{Binding Entries, RelativeSource={RelativeSource AncestorType=UserControl}}"
              AutoGenerateColumns="False"
              CanUserAddRows="True"
              CanUserDeleteRows="True"
              HeadersVisibility="Column"
              Background="#1A1A1A"
              Foreground="White"
              Height="200"
              MinRowHeight="28"
              RowHeaderWidth="0"
              GridLinesVisibility="Horizontal"
              HorizontalGridLinesBrush="#22FFFFFF"
              VerticalGridLinesBrush="#22FFFFFF"
              BorderBrush="#44FFFFFF"
              BorderThickness="1">
        
        <DataGrid.Resources>
            <SolidColorBrush x:Key="HeaderBackground" Color="#2A2A2A"/>
            <SolidColorBrush x:Key="RowBackground" Color="#1F1F1F"/>
            <SolidColorBrush x:Key="BorderBrush" Color="#44FFFFFF"/>
            <SolidColorBrush x:Key="LineBrush" Color="#22FFFFFF"/>
            <SolidColorBrush x:Key="EditBackground" Color="#222222"/>
            <SolidColorBrush x:Key="EditBorder" Color="#66FFFFFF"/>
        </DataGrid.Resources>

        <DataGrid.ColumnHeaderStyle>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="{StaticResource HeaderBackground}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                <Setter Property="Padding" Value="4"/>
            </Style>
        </DataGrid.ColumnHeaderStyle>

        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="{StaticResource RowBackground}"/>
                <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            </Style>
        </DataGrid.RowStyle>

        <DataGrid.CellStyle>
            <Style TargetType="DataGridCell">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="{StaticResource RowBackground}"/>
                <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
                <Setter Property="Padding" Value="8,5,8,5"/>
            </Style>
        </DataGrid.CellStyle>

        <DataGrid.Columns>
            <DataGridTextColumn Header="Key" Binding="{Binding Key, UpdateSourceTrigger=PropertyChanged}" Width="*">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
                <DataGridTextColumn.EditingElementStyle>
                    <Style TargetType="TextBox">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="{StaticResource EditBackground}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource EditBorder}"/>
                    </Style>
                </DataGridTextColumn.EditingElementStyle>
            </DataGridTextColumn>

            <DataGridTextColumn Header="Value" Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="2*">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
                <DataGridTextColumn.EditingElementStyle>
                    <Style TargetType="TextBox">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="{StaticResource EditBackground}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource EditBorder}"/>
                    </Style>
                </DataGridTextColumn.EditingElementStyle>
            </DataGridTextColumn>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>